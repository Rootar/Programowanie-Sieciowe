using Chat.src.network;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Net;
using System.Net.Sockets;
using System.Text;
using System.Threading;
using System.Threading.Tasks;

namespace Chat
{
    class Program
    {
        //public static string nickname = "";
        //public static MulticastListener multicastListener;
        //public static MulticastSender multicastSender;
        static void Main(string[] args)
        {
            //multicastListener = new MulticastListener();
            //multicastSender = new MulticastSender();

            //Thread listener = new Thread(new ThreadStart(multicastListener.RunListener));
            //Thread sender = new Thread(new ThreadStart(multicastSender.RunSender));

            //while (true)
            //{
            //    Console.Write("Podaj nickname: ");
            //    nickname = Console.ReadLine();

            //    multicastSender.RegisterNickname(nickname);

            //    if (multicastListener.CheckIfNicknameIsAvailable() == true)
            //        break;
            //}

            //listener.Start();
            //sender.Start();

            //while (true) {}
        }
    }

    
}
